type MusicProps = {
    id: string,
    title: string,
    author: string,
    lyrics: string | null | undefined,
    image: string,
    category: string,
    publishedDate: string,
    src: string
}

export default MusicProps;